import path from "path";

export const inMemoryDataPath = path.join(
    __dirname,
    "../repositories",
    "data.json"

    /*
        __dirname,
        "..",
        "..",
        "..",
        "data.json"
    */
);